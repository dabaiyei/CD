# AI 短剧制作平台需求基线

> 文档状态：需求收集中  
> 最后更新：2026-09-03
> 说明：本文档记录当前已经确认的产品需求。尚未说明的功能不得自行视为已确认需求，后续讨论后持续补充。

## 1. 产品定位

建设一套 AI 短剧制作平台，采用多租户 SaaS 模式，为普通用户提供短剧项目创作能力，并为管理员提供模型平台、Agent、提示词、Skills 和系统能力的统一管理后台。

## 2. 技术与终端要求

- 前端使用 Vue 3。
- 后端使用 Python。
- UI 使用较新的、同时适配 PC 和 H5 的框架。
- 系统采用多租户 SaaS 架构。
- 不同租户之间的数据、配置和权限必须隔离。

### 2.1 Web 端设计定位

- Web 端必须达到成熟商业 SaaS 产品的视觉与交互质量，不能直接使用 UI 框架默认主题拼装页面。
- 设计方向参考[即梦 AI 创作平台](https://jimeng.jianying.com/ai-tool/home)的产品气质，包括低噪声工作区、清晰的功能导航、内容优先、突出的核心创作入口和克制的视觉层级。
- 只参考其信息组织和产品气质，不复制其品牌标识、图片素材、具体页面或交互实现。
- 平台首先是高频使用的创作工具，不采用营销落地页式的大标题、装饰性模块堆叠或过度留白。
- 整体风格需要专业、现代、精致、稳定，兼顾 AI 创作产品的想象力和生产工具的效率。

### 2.2 视觉系统

- 建立平台自己的 Design Token，包括颜色、字体、字号、间距、圆角、阴影、层级、动效时长和响应式断点。
- 以高质量中性色作为主要工作区底色，使用克制的品牌强调色，并为成功、警告、失败、处理中等状态配置明确的语义色。
- 避免整站被单一蓝色、紫色或渐变主导，不使用装饰性渐变光球、模糊色块和廉价玻璃拟态。
- 页面区块以无框布局和自然分组为主；卡片只用于项目、资产等重复条目和确实需要边界的工具。
- 卡片圆角原则上不超过 `8px`，嵌套圆角遵守同心关系，避免内外层使用相同圆角。
- 卡片、按钮和浮层通过低透明度多层阴影建立层级；表格分隔、输入框焦点等功能性边界保留清晰描边。
- 项目封面、资产缩略图等图片使用不影响布局的低透明度中性轮廓，确保浅色与深色图片都有稳定边界。
- 图标优先使用统一图标库，常见操作优先使用行业通用图标，并为不熟悉的图标提供 Tooltip。
- 不允许为能够使用图标表达的常见工具操作设计大块文字按钮。

### 2.3 排版与信息密度

- 使用适合中英文混排的现代无衬线字体栈，并保持清晰的字号、字重和行高层级。
- 工作台、后台和导演创作页使用适合长期操作的中高信息密度，不使用营销页式超大字号。
- 标题使用均衡换行，项目说明、状态说明和卡片文本使用更自然的防孤行换行策略。
- 动态积分、任务数量、进度、金额和时长使用等宽数字，避免数据变化导致布局跳动。
- 所有按钮、标签、卡片标题和表格内容在 PC 与 H5 下不得溢出、遮挡或挤压其他内容。

### 2.4 PC 端布局原则

- PC 端采用稳定的应用外壳，包括主导航、租户或账号上下文、内容工作区和必要的任务状态入口。
- 普通用户创作台以项目浏览和快速进入创作为核心，不把项目列表包装成多层嵌套卡片。
- 管理后台强调扫描、筛选、比较和批量维护效率，采用紧凑表格、列表、分栏编辑器和抽屉等工作型布局。
- 导演创作页后续采用工作台式布局，支持稳定的项目上下文和可扩展的多阶段创作区域，具体功能待后续定义。
- 宽屏需要设置合理的内容最大宽度或可扩展工作区，不能把内容无限拉宽，也不能在大屏中央只留下狭窄内容列。

### 2.5 H5 响应式原则

- H5 必须是独立设计过的响应式体验，不能简单缩放、裁切或横向滚动 PC 页面。
- PC 侧边导航在 H5 下转换为底部导航、顶部入口或抽屉，具体方式按页面任务密度选择。
- 项目列表在 H5 下采用单列或适合窄屏的紧凑布局；复杂表格转换为摘要列表、分步页面或详情抽屉。
- PC 弹窗在 H5 下根据内容转换为底部面板或全屏页面，不能出现超出视口的固定宽度弹窗。
- 触摸操作目标至少为 `40px × 40px`，优先达到 `44px × 44px`，且相邻点击区域不能重叠。
- 表单在窄屏下保持标签、错误信息和操作按钮完整可见，并适配软键盘、安全区和横竖屏变化。
- 核心用户流程需要在 `360px`、`390px`、`430px` 宽度下无横向溢出；PC 重点验证 `1280px`、`1440px` 和 `1920px` 宽度。
- 移动端禁止用户通过双击或双指手势缩放网页，创作工作区仍需保持正常滚动、软键盘和安全区行为。
- Web 支持 PWA 安装到桌面或移动设备主屏幕，并以 `standalone` 独立应用模式运行。
- 支持 Chromium 浏览器原生安装提示；iPhone 和 iPad 提供 Safari“添加到主屏幕”操作指引，已经安装后不再重复提示。
- PWA 必须提供标准应用清单、`192x192`、`512x512`、Maskable 和 Apple Touch 图标，以及可自动更新、不会缓存业务 API 或用户上传媒体的 Service Worker。

### 2.6 交互与动效

- 动效用于表达层级、状态变化和操作反馈，不能为了装饰持续播放或干扰创作。
- Hover、展开、切换、抽屉和状态变化优先使用可被用户中断的 CSS Transition。
- 页面进入动效按标题、内容和操作区域进行轻量分段；退出动效比进入更短、更弱。
- 普通按钮按下反馈使用 `scale(0.96)`，不适合缩放的关键提交或静态按钮允许关闭该效果。
- 状态图标切换使用透明度、缩放和轻微模糊过渡，不突然替换造成视觉闪烁。
- 禁止使用 `transition: all`；只允许声明实际变化的属性。
- `will-change` 只在确认存在首帧卡顿时用于 `transform`、`opacity` 或 `filter`，不能全局滥用。
- 尊重系统的减少动态效果设置，在 `prefers-reduced-motion` 下关闭非必要动效。

### 2.7 完整产品状态

每个业务页面和可异步操作必须设计完整状态，包括：

- 首次加载和骨架屏。
- 空数据状态。
- 权限不足状态。
- 配置缺失状态。
- 请求失败和可重试状态。
- AI 任务排队、生成中、成功、失败、取消和重试状态。
- 积分不足及扣费说明状态。
- 图片或视频加载占位、渐进显示和资源失效状态。

### 2.8 可用性与性能验收

- 支持键盘导航、清晰的焦点状态、合理的语义标签和足够的文字对比度。
- 交互过程不得出现无意义布局跳动；固定格式的工具栏、计数器、网格和媒体区域需要稳定尺寸。
- 普通页面在生产环境目标为 `LCP < 2.5s`、`INP < 200ms`、`CLS < 0.1`，大型生成资产采用懒加载和渐进展示。
- 列表、项目和生成资产数量较大时必须采用分页、虚拟滚动或增量加载，不能一次渲染全部内容。
- 上线前必须通过 PC 与 H5 的真实浏览器截图、交互和溢出检查，不能只依赖组件级开发预览。

## 3. 用户角色与权限

### 3.1 管理员

管理员可以登录管理后台，对以下高级能力进行配置和维护：

- AI 平台和模型。
- 默认模型。
- 剧本 Agent。
- 通用 AI。
- 提示词。
- Skills 技能文件。
- 视觉手册。
- 导演手册。
- Agent 记忆相关能力。

### 3.2 普通用户

- 登录后进入自己的创作台。
- 只能访问所属租户内、本人有权限访问的项目和数据。
- 可以使用管理员预先配置的模型、Agent、提示词、Skills、视觉手册和导演手册。
- 不能进入管理员管理后台。
- 不能查看或修改上述高级配置，使用过程应尽量无感。

### 3.3 管理员邀请注册

- 租户管理员可以生成唯一邀请码，并为每个邀请码设置注册人数上限和每位新用户的初始积分。
- 邀请码支持启用、停用、编辑和软删除；删除后公开链接立即失效，但历史兑换记录、用户来源和积分流水必须保留。
- 管理员可以在邀请注册模块单独配置公开邀请链接前缀。前缀只允许有效的 HTTP 或 HTTPS 地址，不允许携带查询参数或片段。
- 管理端支持复制邀请码、复制完整邀请链接，以及在浏览器本地生成、复制或下载邀请二维码；未配置链接前缀时不得生成可分享链接。
- 邀请注册页为独立公开页面，支持可选头像和必填邮箱、名称、密码；注册成功后自动签发访问令牌和 HttpOnly 刷新会话并进入创作台。
- 头像只接受经过服务端验证和规范化的 JPG、PNG、WebP 文件，大小及像素限制与系统头像规则一致。
- 邀请名额占用、用户创建、积分账户、初始积分流水、邀请码兑换记录和刷新会话必须在同一数据库事务中完成。并发注册必须使用数据库条件更新防止超发，失败或重复邮箱不得消耗名额。
- 邀请码、兑换记录、注册用户和管理员操作必须按租户隔离；普通用户不能访问邀请码管理接口。

## 4. AI 平台与模型管理

### 4.1 平台接入

管理员可以接入和管理不同类型的 AI 平台，包括：

- Sub2API。
- New API。
- 自定义平台类型。
- 兼容 OpenAI API 格式的平台。

### 4.2 模型分类

系统支持以下四类模型：

- 文本模型。
- 图片模型。
- 视频模型。
- TTS 模型。

### 4.3 平台与模型测试

- 管理员可以测试平台连接是否正常。
- 管理员可以测试相关模型是否可用。
- 测试结果需要明确展示成功或失败状态，并在失败时提供可诊断的信息。

### 4.4 默认模型规则

以下默认模型必须配置，系统才能正常使用全部功能：

- 默认文本模型。
- 默认视频模型。
- 1K 图片模型路由。
- 2K 图片模型路由。
- 4K 图片模型路由。

每档图片分辨率由管理员独立选择 API 平台及其指定图片模型。系统中的项目封面、资产生图、导演流程补齐资产和首页图片生成必须按实际请求分辨率自动路由，普通用户不直接配置或感知底层图片供应商。

默认 TTS 模型为可选配置。

## 5. Agent 与记忆

### 5.1 剧本 Agent

管理员可以动态配置剧本 Agent。其职责包括但不限于：

- 读取原文。
- 生成故事骨架。
- 生成或执行改编策略。

### 5.2 通用 AI

管理员可以配置通用 AI，用于辅助和边缘功能，包括但不限于：

- 小说事件提取。
- 资产提示词生成。
- 台词提取。

### 5.3 Agent 记忆

- 系统必须支持 Agent 记忆的持久化保存。
- 记忆数据必须遵守租户、用户及项目的数据隔离要求。
- 每轮对话完成后自动调用当前文本模型提炼稳定事实，过滤寒暄、临时指令、敏感凭据和可直接从项目文件读取的全文。
- 自动记忆保留命名空间、稳定键、重要度、来源会话、来源消息、提炼任务和向量版本，支持与手工记忆区分。
- 记忆使用项目级向量检索并结合重要度和时效排序，只把与当前意图相关的有限结果注入 Agent，不再固定加载最近 20 条。
- PostgreSQL 生产环境使用 pgvector 与 HNSW 余弦索引；SQLite 开发环境使用兼容向量存储与同等排序逻辑。
- 每轮生成版本化跨轮摘要。AgentScope 状态丢失时使用最新摘要和最近消息恢复，正常连续会话不重复注入已存在于 Runtime 状态中的历史。
- AgentScope 使用稳定聊天会话 ID，并将状态与最后完成任务回执原子保存；同一任务重试不得重复执行或重复追加一轮。
- 删除对话时同步删除该会话摘要、由该会话派生的自动记忆以及 Runtime 状态；手工项目记忆不受影响。

## 6. 提示词管理

管理员可以在管理后台维护不同业务功能使用的提示词，当前至少包括：

- 事件提取提示词。
- 剧本资产提取提示词。
- 视频提示词生成提示词。
- 音色绑定提示词。

提示词体系需要保留继续扩展其他业务类型的能力。

## 7. Skills 技能管理

管理员后台提供 Skills 文件管理页面。

- 页面以文件夹管理器形式展示。
- 只显示指定 Skills 根目录及其下的子文件夹和文件。
- 管理员可以选择文件并查看其内容。
- 管理员可以编辑和保存文件。
- 必须限制文件访问边界，禁止通过路径穿越等方式读取或修改 Skills 根目录之外的文件。

### 7.1 用户技能、模板与素材广场

- 首页主导航增加所有登录用户可见的技能广场、模板广场和素材广场，PC 与 H5 均需提供完整入口。
- 用户可以将自己的 Skill 主动分享到技能广场。发布内容为当前版本的公开快照，不允许广场接口直接读取发布者后续修改的私人源对象。
- 其他用户可以一键复制公开 Skill 到“我的 Skills”；复制后形成归当前用户所有的独立 Skill，可编辑、启停和删除，并参与原有阶段检索流程。
- 作者修改源 Skill 后需要再次发布才生成广场新版；已复制用户看到新版提示后主动同步，不允许作者更新静默覆盖使用者副本。
- 模板广场支持用户创建、编辑、删除和发布私人文本模板，其他用户可以一键复制到自己的模板库并独立维护。
- 素材广场支持用户发布自己创建的全局人物、场景、道具、素材或音频资产；其他用户一键导入到本人所属租户的全局资产库。
- 技能、模板和素材广场可以跨租户展示用户主动发布的快照，但私人源对象、项目资产、租户配置和模型凭据仍必须隔离。
- 广场支持搜索、分类、最新和热门排序、发布者信息、版本、复制次数、详情、下架和显式同步新版。
- 删除源 Skill、私人模板或源全局资产时自动下架对应广场条目，但不得删除其他用户已经复制的独立副本。

## 8. 普通用户创作台

普通用户登录后，首页显示用户自己的创作台。创作台展示用户当前有权访问的短剧项目，每个项目代表一个独立的短剧项目。

### 8.1 项目基础信息

每个项目至少包含：

- 项目名称。
- 项目简介。
- 项目封面图。

### 8.2 项目封面图

项目封面支持以下来源：

- 未设置封面时显示系统默认项目图。
- 用户自行上传图片。
- AI 根据项目名称、项目简介和项目选择的画风智能生成图片。

使用 AI 生成项目封面需要消耗用户积分，并纳入积分扣减与流水记录。

### 8.3 创作台 Agent 对话

- 创作台提供以大输入区为核心的 Agent 对话入口，支持 PC 与 H5。
- 用户必须在本人有权访问的短剧项目内发起对话；系统根据创作台或导演台场景自动路由管理员启用的对应 Agent，普通用户不选择 Agent。
- 对话自动加载项目绑定的视觉手册、导演手册和平台保存的 Agent 长期记忆。
- 普通用户只能查看 Agent 和项目技能的名称与说明，不能查看或修改系统提示词、模型密钥和 Skills 文件。
- 会话和消息由平台数据库持久化，并按租户、用户和项目隔离；AgentScope 状态不能替代平台会话记录。
- Agent 文本运行必须通过独立的 AgentScope Runtime，不允许由前端直连模型平台。
- 每轮对话提交后立即返回 PostgreSQL 持久任务，不用长时间 HTTP 请求等待 Agent 完成；同一会话同时只允许一个排队中或进行中的任务。
- 平台数据库中的用户和助手消息、版本化摘要和项目记忆是多轮恢复的权威来源。AgentScope 使用稳定会话状态承接正常跨轮上下文，并通过原子任务回执保证重试和重启恢复不会重复执行同一轮。
- Worker 必须持久化 Agent 的真实进度事件、最终回复、Runtime 清单和项目文件变更结果；页面刷新后能够恢复排队或运行状态，成功、失败和取消均同步到统一任务中心。
- 创作台支持新建对话、删除对话、继续历史对话、切换项目和查看当前项目自动启用的技能手册；Agent 由场景自动选择。

## 9. 项目生成配置

每个短剧项目需要配置或绑定以下内容：

- 视频模型。
- 视频模型的生成分辨率。
- 视频模型的影片比例。
- 图片模型的生成分辨率。
- 视觉手册。
- 导演手册。

图片生成分辨率固定提供以下三个选项：

- 1K。
- 2K。
- 4K。

不同项目可以使用不同的视频模型、分辨率、比例、视觉手册和导演手册。图片模型由管理员按图片分辨率统一配置，项目仅选择图片分辨率。

## 10. 视觉手册

视觉手册用于约束当前项目后续生成内容的画风和视觉一致性。视觉手册本质上是由管理员在后台单独配置的一套 Skills 技能包，由多个文件组成。

### 10.1 视觉手册元数据

每套视觉手册包含可编辑的：

- 名称。
- 说明。
- 封面图。

上述内容由管理员维护，普通用户在项目中选择并使用视觉手册。

### 10.2 视觉手册技能包组成

每套视觉手册包含以下组成部分：

| 组成部分 | 用途 |
| --- | --- |
| `README.md` | Skills 画风主说明文件 |
| 前缀 | 画风全局美学基础文件 |
| 角色 | 人物基础形象生成的约束手册 |
| 角色衍生 | 人物衍生资产生成约束 |
| 道具 | 道具资产形象生成约束 |
| 道具衍生 | 道具衍生资产生成约束 |
| 场景 | 场景资产形象生成约束 |
| 场景衍生 | 场景衍生资产生成约束 |
| 分镜 | 导演分镜提示词技法 |
| 分镜视频 | 视频提示词的视觉风格约束 |
| 技法-导演规则 | 对色调体系、光影方案、质感方向、场景空间元素、乐器选择和环境音进行全局约束，适用于叙事类内容 |
| 技法-分镜表设计 | 规范分镜表中的光影氛围、光源角度、动作节奏、环境动态、运镜禁忌和转场禁忌，适用于叙事类内容 |

具体目录结构、文件名和文件格式将在实现前进一步确认。

## 11. 导演手册

导演手册用于约束当前项目的叙事手法，本质上是由管理员在后台配置的一套 Skills 技能包。

### 11.1 导演手册元数据

每套导演手册包含可编辑的：

- 名称。
- 说明。
- 封面图。

上述内容由管理员维护，普通用户在项目中选择并使用导演手册。

### 11.2 导演手册技能包组成

每套导演手册包含以下组成部分：

| 组成部分 | 用途 |
| --- | --- |
| `README.md` | 导演在叙事规划阶段使用的技法参考 |
| 导演规划 | 导演规划相关技能内容 |
| 分镜表 | 分镜表设计和生成相关技能内容 |

具体目录结构、文件名和文件格式将在实现前进一步确认。

## 12. 导演创作页

- 用户在创作台点击项目后，进入该项目对应的导演创作页。
- 导演创作页是项目内部的核心工作区。
- 页面必须校验当前用户是否拥有该项目的访问权限，并保持租户和项目上下文隔离。

### 12.1 内容导入与章节

- 支持小说、剧本两种导入模式。
- 两种模式均支持 TXT、EPUB 文件上传，也支持直接粘贴文本。
- 系统智能识别章节或集，并保存用户上传的原始文件、EPUB 提取后的可编辑文本及章节原文。
- 新章节初始状态为“未初始化”，用户选择章节后才能与 AI 协作推进生产流程。

### 12.2 章节生产流程

每个章节按“原文分析与改编 -> 剧本生成 -> 审核 -> 资产提取 -> 分镜 -> 视频 -> 台词与配音”推进。步骤之间存在明确前置条件，不允许绕过前置数据直接执行下游任务。

- 剧本、资产提取结果和分镜均保留历史版本。
- 一个章节同一时间只能有一个生效剧本版本。
- 切换生效剧本后，依赖旧剧本的资产提取、分镜和后续内容必须标记失效并重新生成，但不能删除历史记录。
- AI 操作需要记录输入版本、模型、提示词、Skills、输出和任务状态，以便审计与复现。
- 章节分析必须作为正式持久任务执行，生成摘要、核心冲突、开场钩子、事件链、人物动机、改编策略和审核风险，并保留多个历史版本。
- 每个章节分析版本必须写入项目文件库；原文在任务执行期间变化时，旧分析结果不得覆盖新内容。
- AI 剧本生成可绑定指定分析版本和参考剧本版本，完成后创建新的“待审核”剧本版本，不得自动替换生效稿。
- AI 剧本任务取消或失败后必须恢复章节到最近有效阶段；失败任务退款，原文或参考版本失效时禁止重试。
- 审核步骤必须保存不可变决策记录，包括剧本版本、审核人快照、通过或退回结果、审核意见、时间和是否触发生效；退回修改必须填写意见，生效中的剧本不得直接退回。
- 审核通过可以原子地将该剧本设为章节生效版本，并复用剧本切换时的下游失效规则，确保资产、分镜、视频和配音不会继续引用旧剧本。
- 章节分析和 AI 剧本使用当前租户的可配置计费规则；默认分别为 `5` 积分/版本和 `10` 积分/版本，任务创建后价格不得随规则修改而变化。

#### 12.2.1 全局导演 Agent 编排

- 导演台全局只显示一个导演 Agent 对话区，切换章节不创建第二个对话面板；普通用户不能选择具体 Agent。
- 主 Agent 读取章节原文、项目文件、项目记忆、通用剧本 Skills、视觉手册和导演手册，并通过持久化状态机推进章节，不允许只依赖前端提示词串联步骤。
- 剧本改编、剧本审核、剧本修复、资产提取、资产提示词、资产生图、分镜生成、分镜审核和分镜修复均保存为可追踪的子智能体执行记录。
- 子智能体记录父流程、父子任务、输入版本、任务 ID、输出引用、运行清单、当前尝试次数和失败原因；Web 端以默认收起的特殊卡片显示，展开后才显示审核问题和执行细节。
- 子智能体失败后自动重试，最多三次；超过上限时流程进入明确失败状态，不得静默跳过。
- 剧本生成后必须自动调用独立审核子智能体。审核不通过时，主 Agent 必须给出“局部修复、整版重做、忽略问题继续、补充意见后修复”四类结构化选择，不能依赖解析任意自然语言来决定状态迁移。
- 用户选择修复后，主 Agent 将任务下发给修复子智能体，完成后再次自动审核；每次重新审核不通过都必须重新请求用户决定。
- 剧本审核通过或用户明确选择继续后，系统自动设为生效剧本并触发资产提取；资产提取完成后暂停自动流程，允许用户检查、编辑和手工生成资产。
- 分镜只能从导演 Agent 流程发起。开始前必须读取当前资产提取版本；除音频外，必需资产以图片文件实际存在作为完成依据。
- 缺少提示词或图片的资产由子智能体自动补全提示词并创建持久图片任务；所有必需资产验证通过后才能生成分镜。
- 分镜生成后必须自动调用独立审核子智能体，审核不通过时沿用局部修复、整版重做、继续和补充意见的决策循环；通过后进入待视频阶段。
- 视频生成不属于本阶段实现范围，但流程与数据模型必须保留 `ready_for_video` 终态供后续接入。

### 12.3 项目文件库

- 每个项目拥有独立文件库，包含初始上传内容、项目记忆、章节分析、剧本和后续生产文件。
- 用户可以新增、读取、编辑、下载和删除有权限的文件；不可编辑的原始文件仍可下载。
- 项目 AI 可以在权限范围内读取和编辑项目文件。
- 文件必须按租户、用户和项目隔离，文件名与存储路径不能用于绕过目录边界。
- Agent 运行时只接收本次运行所需的文本文件快照，不直接访问 PostgreSQL 或共享上传目录；运行结束只返回结构化新增、修改和删除意图。
- 核心后端必须重新校验租户、用户、项目、文件可编辑性、扩展名、大小和运行前 SHA-256；并发修改返回冲突，不得覆盖用户的新版本。
- Agent 不得删除来源文件或不可编辑文件。每项应用、冲突和拒绝结果必须写入 Agent 消息运行清单并在 Web 会话中可见。

### 12.4 资产库

- 资产类型包括人物、场景、道具、素材和音频；人物、场景、道具支持衍生资产。
- 衍生关系只允许一级结构，子项必须与基础资产类型一致且位于同一个项目库或全局库；素材、音频及衍生资产本身不能继续拥有子项。
- 用户可以在创建或编辑人物、场景、道具时选择基础资产；已有衍生项的基础资产不得直接删除或降级为其它资产的衍生项，必须先处理子项。
- “塑造资产”属于单个项目，项目 AI 可以读取、引用和控制，项目生产优先使用该库。
- “全局资产库”在租户内共享，但项目 AI 不能直接读取或引用；必须先由用户复制导入当前项目。
- 项目资产和全局资产支持双向复制导入导出，复制结果保持独立，不能形成跨项目隐式修改；复制衍生资产时必须自动复制或复用目标库中的对应基础资产并保持谱系，不得产生悬空关系。

### 12.5 资产提取与生成

- 资产提取必须基于当前章节的生效剧本版本。
- 首次提取只建立人物、场景、道具及其衍生资产的名称、类型和资产说明等基础数据。
- 用户检查提取结果后，可对单个、多选或全选资产调用 AI 生成生图提示词。
- 只有提示词生成成功的资产才能创建图片生成任务。
- 同一章节不能同时运行多个资产提取任务；同一资产不能同时运行多个提示词或图片任务。
- 资产提取结果必须保留版本、来源剧本版本、任务 ID 和运行时清单，并写入项目文件库供用户下载和 AI 审计读取。
- 提示词任务必须记录资产版本；执行期间资产被用户编辑时，旧任务结果不得覆盖新版本。
- 每个资产必须保留不可变历史快照，覆盖手工创建与编辑、AI 提取、AI 提示词生成、图片生成和项目/全局资产库复制，并记录变更来源与关联任务。
- 资产历史版本只能预览或恢复为新的递增版本，禁止原地覆盖或删除旧快照；恢复衍生关系时仍须校验同租户、同资产库、同类型和一级衍生约束。
- 图片生成按资产拆分为独立持久任务，便于单项取消、失败重试、积分结算和重启恢复；音频资产不得进入图片生成任务。
- 已有定稿图的资产重新生成失败或取消时保留旧图片；首次生成失败才将资产标记为失败。
- 资产提取、提示词和资产图片使用当前租户的可配置计费规则；默认分别为 `8` 积分/版本、`2` 积分/资产和 `20` 积分/张。

### 12.6 分镜与单镜头视频

- 分镜生成必须绑定当前生效剧本，以及由该剧本生成的生效资产提取版本；任一前置版本切换后旧任务结果不得落库。
- 分镜以章节版本保存，每个版本包含有序镜头实体，并同步生成可下载、可审计的 JSON 项目文件。
- 镜头至少保存标题、景别、时长、场景、动作与运镜、台词、图片提示词、视频提示词、资产引用和参考图。
- 用户可查看历史分镜并切换生效版本；同一章节只能有一个生效分镜，切换后其它分镜的视频不再生效但保留历史。
- 编辑镜头会增加镜头修订版本并失效其旧视频，执行中的旧镜头任务不得覆盖新修订。
- 视频按单镜头创建独立持久任务和独立历史片段；重复生成失败时保留旧的生效视频。
- 视频平台返回异步任务时必须立即持久化服务商任务 ID；Worker 重启后从该 ID 继续轮询，不得重复提交生成。
- 服务商任务仍可查询时，网络中断、下载失败或本地持久化失败后的重试必须保留服务商任务 ID 并继续查询；只有服务商明确返回终止失败时才能清除旧任务 ID 并创建新的生成尝试。
- 生成成功的视频写入项目文件库；排队取消、失败退款、重试和版本失效必须同步更新任务与视频片段状态。
- AI 分镜和单镜头视频使用当前租户的可配置计费规则；默认分别为 `12` 积分/版本和 `60` 积分/镜头。

### 12.7 台词提取、角色音色与配音

- 台词提取必须绑定当前生效剧本，并可选绑定当前生效分镜；提取结果按章节保留多个历史版本，同一章节只有一个生效台词版本。
- 每条台词至少保存角色、正文、情绪、表演指导、可选镜头映射、来源哈希和修订版本，并生成可下载、可审计的 JSON 项目文件。
- 项目人物资产可绑定租户内启用的 TTS 模型、服务商音色 ID、显示名称、表演风格和补充指令；绑定修改后版本递增并失效旧配音。
- 角色名与人物资产名称一致时才能自动应用音色；缺少人物资产或音色绑定的台词不得创建配音任务，并明确提示缺失角色。
- 配音按台词拆分为独立持久任务和独立音频片段，支持逐条或批量创建、失败退款、取消、重试、版本历史和生效片段。
- 异步 TTS 必须在轮询前持久化服务商任务 ID，Worker 重启后从该 ID 恢复；同步 TTS 在重启时无法证明上游结果且没有任务 ID 时必须失败退款，等待用户显式重试。
- 台词、剧本、分镜或音色绑定变化时，受影响旧音频只停止生效并保留历史；旧任务结果不得覆盖新版本。
- TTS 由平台媒体网关执行，不交给 AgentScope 直接调用、扣费或管理业务状态；成功音频写入项目文件库。
- 台词提取和单条配音使用当前租户的可配置计费规则；默认分别为 `4` 积分/版本和 `5` 积分/条。

## 13. AgentScope 深度融合方案

AgentScope 作为系统中的独立 Agent Runtime 使用，负责文本 Agent 的 ReAct 循环、原生异步模型调用、受控工具调用、Skills 装载、上下文压缩、状态快照和运行事件，不作为整个 SaaS 系统的后端框架。

### 13.1 系统职责边界

| 组件 | 主要职责 |
| --- | --- |
| Vue 3 前端 | 用户创作台、导演创作页和管理员后台 |
| Python SaaS 核心后端 | 多租户、账号、权限、项目、积分、模型配置、提示词、手册、资产和业务 API |
| 业务工作流与任务队列 | 编排可审计、可重试、可计费的短剧生产任务 |
| 媒体模型网关 | 图片生成、视频生成和 TTS 调用及供应商适配 |
| Agent Runtime 服务 | 对内提供统一、版本化 Agent 运行接口，并隔离 AgentScope 实现 |
| AgentScope | ReAct Loop、OpenAI 兼容文本模型调用、工具、Skills、上下文压缩和原生事件 |

AgentScope 不负责以下系统核心能力：

- SaaS 租户和账号体系。
- RBAC 权限校验。
- 项目和业务数据的最终存储。
- 积分计费及账务一致性。
- 图片、视频和 TTS 的统一模型调用。
- 确定性的短剧业务工作流编排。

### 13.2 独立部署与目录边界

- AgentScope 必须放在独立的 Agent Runtime 服务中，不能直接散落或耦合到 Python 核心业务模块。
- 正式目录为 `services/agent-runtime/agentscope/`，用于存放运行时封装、配置、安全策略、兼容代码、容器构建和测试；旧 `deepseek-harness` 目录只保留作历史回滚参考。
- Python 后端只通过平台自有的 `AgentRuntime` 异步客户端访问该服务，不直接依赖 AgentScope 内部类型和存储格式。
- Agent Runtime 与 Python 后端之间使用版本化内部 HTTP 协议，当前正式协议为 `/internal/v2/runs`。
- Agent Runtime 只部署在内部受控网络，不直接作为公网用户入口。

### 13.3 上游依赖与定制规则

- 不把 AgentScope 上游仓库源码直接复制进业务代码。
- 不直接修改上游核心源码来实现平台业务需求。
- 使用明确锁定的 `agentscope==2.0.7.post1`，禁止在生产环境使用不固定的 `latest` 版本。
- 平台定制能力通过自有适配器、工具中间件、Skills 转换和版本化协议实现。
- 必须使用上游补丁时，补丁集中保存在 Agent Runtime 目录并记录适用的上游版本和原因。
- AgentScope 自带的 Service 或 UI 不作为本平台用户端、管理端或租户权威服务使用。

### 13.4 Skills 与手册融合

- 视觉手册和导演手册的权威数据由 Python 核心后端及平台存储管理。
- Agent 运行前，由 Agent Runtime 根据租户、项目和手册版本动态装载所需 Skills。
- 为适配本平台以 `README.md` 和多个业务文件组成的技能包，Runtime 在任务快照内生成带 `name`、`description` Front Matter 的标准 `SKILL.md`，原始文件作为只读资源保留。
- 不允许不同租户通过共享的可写本地 Skills 目录互相读取或覆盖内容。
- 每次任务必须记录实际使用的视觉手册版本、导演手册版本和 Skills 内容版本，以支持追踪和复现。

### 13.5 会话、记忆与数据归属

- 平台业务数据库是用户、租户、项目、任务、积分和资产数据的唯一权威来源。
- AgentScope 状态和事件用于 Agent 执行记录、上下文压缩和诊断，不能替代平台业务数据库。
- Agent 长期记忆由平台 PostgreSQL 管理并作为冻结上下文提供给 AgentScope；AgentScope 状态只保存在租户、项目、会话隔离的 Runtime 目录中。
- AgentScope 的状态目录和工作空间必须分别按租户/项目/会话及租户/项目/任务建立明确隔离边界。
- 平台保存的模型平台密钥必须加密，运行时只获取当前任务需要的凭据，不得将全量密钥暴露给 Agent 或 Skills。
- 项目文件以受控快照进入任务工作区，运行时只能在指定项目文件目录内提出变更；PostgreSQL 始终是文件内容和权限的权威来源。

### 13.6 模型与任务边界

- AgentScope 第一阶段只用于文本 Agent 和可选的图片理解输入。
- 图片生成、视频生成和 TTS 仍通过平台自有媒体模型网关执行。
- Agent 可以通过受控工具创建媒体生成任务，但实际调用、排队、扣费、重试和结果落库由平台任务系统负责。
- AgentScope 的动态 Agent 流程不能直接替代平台持久化业务工作流。

### 13.7 独立升级要求

- Agent Runtime 必须能够在不升级 Python 核心后端和 Vue 前端的情况下单独升级或回滚。
- 每次升级只调整 Agent Runtime 中锁定的 AgentScope 版本及必要兼容代码。
- 升级前必须运行接口契约、模型调用、Skills 加载、工具权限、会话恢复和租户隔离测试。
- 任务执行记录必须保存 Agent Runtime 类型、AgentScope 版本、模型平台、模型、提示词版本和 Skills 版本。
- 恢复 AgentScope 状态时必须检查会话标识和版本兼容性；不能安全恢复时应使用 PostgreSQL 权威历史重建新尝试，不能静默复用不兼容状态。
- 部署方式需要支持新旧 Agent Runtime 版本并行、灰度验证和快速回滚。

### 13.8 当前技术风险

- AgentScope 上游仍可能出现破坏性 API 或状态格式变更，因此必须固定版本并通过契约测试升级。
- AgentScope 的 Permission System 不能作为唯一安全边界；平台额外限制工具集合和所有文件路径，并在核心后端复核 Diff。
- Runtime 必须限制并发、超时、容器文件系统、网络和资源使用，模型密钥和完整思维过程不得写入普通用户可见事件。
- AgentScope 内置多租户服务不替代平台租户、计费、任务和媒体能力，必须始终保持适配层隔离。

## 14. 当前强制业务规则

- 系统必须支持多租户及数据隔离。
- 管理员高级配置不能向普通用户开放。
- 默认文本、图片和视频模型是完整使用系统功能的前置条件。
- 默认 TTS 模型可选。
- AI 生成项目封面必须消耗积分。
- 普通用户只能选择和使用管理员配置的视觉手册与导演手册，不能修改技能包内容。
- Skills 文件管理只能操作指定 Skills 根目录内的文件。
- 进入导演创作页前必须校验项目访问权限。
- AgentScope 必须通过独立 Agent Runtime 服务接入，并支持单独升级和回滚。
- Python 核心后端不能直接依赖 AgentScope 的内部存储格式或具体实现。
- 图片、视频和 TTS 任务不能交由 AgentScope 直接承担计费和业务状态管理。

## 15. 暂未定义的内容

以下内容已知存在，但尚未形成详细需求：

- 多租户自助开通、套餐和首个租户管理员体系；现阶段已支持租户内管理员邀请注册。
- 积分充值、套餐额度和任务提交前的预授权冻结策略。
- 模型平台配置是系统级、租户级还是同时支持两级。
- 项目成员及协作权限。
- Agent 记忆的范围、召回策略和清理策略。
- 视觉手册与导演手册的版本管理和项目绑定后的更新策略。
- 模型分辨率和比例选项由平台返回、管理员配置还是系统固定。
- 文件及生成资产所使用的对象存储方案。
- Agent Runtime 内部协议采用 HTTP、gRPC 还是消息队列。
- Agent Runtime 的进程池、并发模型和租户隔离粒度。
- 历史会话跨 AgentScope 版本迁移或兼容运行时保留策略。

## 16. 商业级基础设施与任务通知

### 16.1 数据库与异步接口

- PostgreSQL 是租户、用户、配置、项目、文件、章节、版本、资产、积分、任务和通知的唯一权威业务数据库。
- Redis 用于任务队列、短期协调和事件传输，不能作为任务状态的唯一存储。
- 用户上传的原始 TXT/EPUB 通过异步对象存储接口保存；开发环境可使用本地后端，生产环境使用私有 S3 兼容存储，下载必须经过项目权限校验。
- 生成封面、资产图片、视频、配音与章节成片同样以对象存储为权威文件；数据库只保存稳定对象键，Worker 在 FFmpeg 执行前按需水化本地缓存，节点重启后不得要求重新调用上游生成服务。
- 生产环境生成媒体地址必须携带服务端签发且与对象路径绑定的 HMAC 能力签名，API 与 Worker 使用一致的独立媒体签名密钥，禁止直接访问未签名对象路径。
- 后端接口采用异步数据库与异步网络客户端；文件解析、图片处理等阻塞或 CPU 密集操作必须转入线程池或独立 Worker。
- 生产部署必须使用数据库迁移，不依赖运行时 `create_all` 演进数据结构。
- 当前实现采用 Alembic 管理生产 PostgreSQL 结构；API 容器升级到 migration head 后才启动，API
  与 Worker 均在生产启动时校验数据库 revision，版本缺失或落后时拒绝提供服务。

### 16.2 可恢复任务

- 图片、资产提示词、视频、TTS 和 Agent 等耗时操作统一创建持久化任务。
- 任务至少区分排队中、进行中、已完成、失败和已取消，并记录进度、重试次数、错误、租约、心跳、幂等键和外部服务商任务标识。
- Redis 消息丢失或服务重启后，Worker 必须从 PostgreSQL 扫描可执行任务并恢复入队。
- 存活 Worker 必须周期扫描过期租约并使用数据库行锁跳过其它 Worker 正在恢复的任务，不能依赖所有 Worker 重启后才接管故障任务。
- Worker 领取任务时必须原子写入 Worker 身份、开始时间、心跳和租约到期时间；终态写入完成时间并释放租约。
- API、迁移工具和 Worker 的本地 SQLite、Skills 与上传目录必须锚定项目根目录解析，不得因进程启动目录不同而连接另一数据库或读写另一资源目录；Worker 启动日志必须输出脱敏数据库目标并强制校验迁移版本。
- Worker 使用可配置的异步执行槽并发领取和执行任务；每个供应商拥有 `1-64` 的最大并发工作流配置，达到上限的任务继续保持排队，且不得阻塞其他供应商任务。并发裁决必须由 PostgreSQL 事务与供应商行锁完成，不能只依赖单进程内存信号量。
- 单个 Agent 对话任务从开始执行到结果持久化最多运行 `300` 秒，超时后必须取消运行时请求、保存已有流快照并持久化为失败；图片、视频、TTS 和渲染等长任务不应用该对话超时。
- Worker 处理任务时使用租约和心跳；租约超时后可安全恢复，旧 Worker 失去所有权后不得写入服务商任务 ID、结果或失败状态，幂等键避免重复扣费和重复提交。
- 同一项目的任务提交必须在数据库事务中串行检查进行中任务；快速重复点击、多端同时提交或多 API 实例并发时不得重复创建同一生产任务或重复扣费。
- 对无法从服务商恢复的同步生成请求，重启后必须明确进入可重试或失败状态并正确结算，不能长期停留在进行中。
- 同步图片、同步视频和同步 TTS 在 Worker 失联后无法证明上游结果时必须失败退款；异步视频和 TTS 只有已持久化服务商任务 ID 才能自动恢复。

### 16.3 消息与任务通知

- 用户拥有统一消息/任务中心，能够查看任务类型、所属项目、状态、进度、开始与完成时间以及失败原因。
- 支持未读/已读、单条重试、可取消任务的取消操作，并在完成或失败时生成持久通知。
- 多 Worker 或多 API 实例下的状态以 PostgreSQL 为准，Redis 只负责加速状态传播。
- PostgreSQL 必须为项目任务判重、租户/用户任务时间线、Worker 租约恢复和未读通知查询维护与实际过滤及排序字段一致的复合索引。
- 任务列表必须展示最新持久化事件的真实百分比和执行消息，不得使用前端模拟进度；任务详情按时间线展示完整事件历史、积分与退款标记。
- PC 端使用紧凑操作面板，H5 使用全屏任务中心，并提供全部、处理中、失败和完成筛选；任务类型使用明确图标与名称区分。
- 登录用户通过带 JWT 请求头的 SSE 接收本人任务事件，禁止在实时连接 URL 中携带访问令牌；Redis Pub/Sub 只负责低延迟传播，前端必须保留低频 PostgreSQL 对账和断线重连。

### 16.4 身份认证

- 使用成熟 JWT 方案：短期访问令牌、HttpOnly 刷新令牌、会话持久化、刷新轮换、注销吊销和重放检测。
- 普通用户和管理员权限在每个后端接口强制校验，不能只依赖前端隐藏入口。
- 登录入口必须具备跨实例 Redis IP 速率限制、PostgreSQL 身份级失败计数和定时锁定；Redis 故障时持久锁定仍须生效。
- 登录成功、失败、锁定和限流事件进入租户安全审计；邮箱和 IP 以不可逆 HMAC 指纹存储，租户管理员只能查看本租户事件。

### 16.5 租户计费规则

- PostgreSQL 按租户和任务类型保存唯一计费规则，是任务扣费和前端价格展示的唯一权威来源；Redis 不保存计费规则或余额真值。
- 初始规则覆盖 Agent 对话、项目封面、章节分析、AI 剧本、资产提取、资产提示词、资产图片、AI 分镜、镜头视频、台词提取和台词配音共 11 类任务。
- 租户管理员可查看和修改本租户单价，普通用户只能通过 `GET /api/v1/pricing` 读取当前租户的有效价格，不能修改规则。
- 任务创建时必须在同一数据库事务内解析规则、校验余额、扣减积分并冻结价格快照；快照至少包含任务类型、单价、数量、总价和规则版本。
- 批量任务按实际数量计算总价；独立媒体任务按单项创建、扣费和结算，不能因批量提交而共享不可拆分的退款状态。
- 修改规则时版本号递增且只影响之后创建的任务；已经创建的任务、重试任务、取消退款和失败退款必须继续使用原始任务价格，不能按最新规则重新计算。
- 零积分任务仍保存价格快照；同一任务只能退款一次，积分余额和流水必须在数据库事务中保持一致。

## 17. 模型平台商业管理规则

- 管理员可以新增、编辑、删除并测试 Sub2API、NewAPI 和自定义 OpenAI 兼容平台。
- 平台支持异步自动获取模型列表；发现结果默认全选，管理员可手动取消、修改模型分类后批量导入。
- 模型覆盖文本、图片、视频和 TTS 四类，支持单独编辑、删除和连通性验证。
- 被默认配置或业务数据引用的平台/模型删除时必须阻止删除并返回可理解的原因。
- 默认文本、图片和视频模型缺一不可；TTS 默认模型可选。
- 核心默认模型就绪校验必须由后端统一任务提交器和项目创建接口强制执行；配置不完整时返回明确的服务不可用错误，且不得创建任务或扣减积分，不能只在管理员页面展示提醒。

## 18. 章节成片与交付

- 生效分镜中的每个镜头必须存在生效视频，才能创建章节合成版本；TTS 及台词音频保持可选。
- 合成版本必须冻结分镜、镜头视频、台词音频、背景配乐、环境音、音量、输出分辨率、比例与帧率，创建后不可覆盖。
- 用户可以上传常用音频格式作为项目配乐或环境音，文件进入项目文件库并受项目和租户权限约束。
- 章节渲染必须作为 PostgreSQL 持久化任务执行，至少支持排队、进行中、完成、失败、取消和重试，并在 Redis 消息丢失或 Worker 重启后恢复。
- 渲染前后都必须校验冻结清单引用的生产版本。分镜、镜头、台词、音色或配音更新时，旧合成版本标记为已失效但保留历史。
- 渲染完成后生成可播放、下载的 MP4 和带完整合成清单的项目文件；同一章节只能有一个生效成片。
